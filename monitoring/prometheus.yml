global:
  scrape_inter  # Node exporter for system metrics
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
ion_interval: 15s
  external_labels:
    monitor: 'skincare-monitor'

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets: []

# Load rules once and periodically evaluate them
rule_files:
  # - "alerts.yml"

# Scrape configurations
scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Node exporter for system metrics
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']

  # Laravel application metrics (if you add prometheus endpoint)
  - job_name: 'laravel-app'
    static_configs:
      - targets: ['skincare.wign.cloud']
    metrics_path: '/metrics'
    scheme: https

  # WhatsApp client metrics (if you add prometheus endpoint)
  - job_name: 'whatsapp-client'
    static_configs:
      - targets: ['whatsapp-client:5555']
    metrics_path: '/metrics'
    scheme: http
